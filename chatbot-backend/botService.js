// botService.js
module.exports.getResponseFromBot = async (userMessage) => {
    // This is where you can add your chatbot logic
    // For now, we're just returning a simple mock response
    return `Bot says: ${userMessage}`;
  };
  